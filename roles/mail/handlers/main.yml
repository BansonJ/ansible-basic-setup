---
# handlers file for mail
- name: Restart postfix
  ansible.builtin.systemd:
    name: postfix
    state: restarted
    enabled: true

- name: Restart dovecot
  ansible.builtin.systemd:
    name: dovecot
    state: restarted
    enabled: true
